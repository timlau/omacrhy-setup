# Setup scripts for doing user specific setup on omarchy

- `setup` run all scripts in the scripts directory
- `add-script NAME` add a new script/00-NAME script with an install command for installing NAME

the order the scripts run in can be adjusted by changing the 00- postfix to the scripts.



